export interface AccountWriteRepository {
  // create(data: CreateAccountDTO): Promise<void>;
}

export interface AccountReadRepository {
  // findById(id: string): Promise<AccountDocument | null>;
  // findByEmail(email: string): Promise<AccountDocument | null>;
}
